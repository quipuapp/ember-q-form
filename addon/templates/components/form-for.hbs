{{yield (hash
  input-for=(
    component "input-for"
      data=data
      showErrors=(readonly showErrors)
  )
  select-for=(
    component "select-for"
      data=data
      showErrors=(readonly showErrors)
  )
  fields-for=(
    component "fields-for"
  )
)}}
